<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="@dimen/activity_horizontal_margin">

    <TextView
        android:id="@+id/textView8"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Cette application a absolument besoin des autorisations suivantes pour fonctionner, vous devez les accorder dans le système Android avant de continuer:" />

    <Button
        android:id="@+id/button2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:onClick="onClickAccorderPermissions"
        android:text="Accorder les autorisations"
        android:textAllCaps="false" />

    <ListView
        android:id="@+id/idListe"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/button2"
        android:layout_below="@+id/textView8"
        android:layout_margin="4dp"
        android:padding="4dp" />
</RelativeLayout>